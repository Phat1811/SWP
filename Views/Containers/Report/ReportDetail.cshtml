@using MedicalStore.Utils.Common
@{
    this.ViewData["Title"] = Routers.ReportList.Title;
    var listReport = (List<ReportTicket>)this.ViewData["listReportByProductId"];
    var product = (Product)this.ViewData["product"];
    var user = (User)this.ViewData["user"];
    
}

<div class="flex-1 min-h-screen px-2 py-2 xl:px-8 xl:py-6">
    <div class="flex">
        @await Html.PartialAsync("../../Components/Navbar/NavbarSide.cshtml")
        <div class="content">
            <div class="mt-5 intro-y box">
                <div class="flex flex-col items-center p-5 border-b border-gray-200 sm:flex-row">
                    <h2 class="mr-auto text-base font-medium">Report of product @product.Name</h2>
                </div>
                @for(int i = 0; i < @listReport.Count; i++)
                {
                    <div>
                        <dl class="mt-2 divide-y divide-gray-200">
                        <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                            <dt class="text-sm font-medium text-gray-500">Customer Name</dt>
                            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">@listReport[i].CustomerId</dd>
                        </div>
                        </dl>
                   </div>
                   <div>
                        <dl class="mt-2 divide-y divide-gray-200">
                        <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                            <dt class="text-sm font-medium text-gray-500">Report</dt>
                            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">@listReport[i].Description</dd>
                        </div>
                        </dl>
                   </div>
                }
                @if(user.RoleId == "1"){
                <div class="min-h-full flex ">
                    <div class="flex-1 flex flex-col justify-center py-12 px-4 sm:px-6 lg:flex-none lg:px-20 xl:px-24">
                        <div class="mx-auto w-full max-w-sm lg:w-96">
                            <a href="/report/create?productId=@product.ProductId" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                                   Create new report
                            </a>
                        </div>
                    </div>
                </div>
                }
            </div>

        </div>
    </div>
</div>

@section Scripts {
        
        <script src="/js/report/create.js"></script>
    }